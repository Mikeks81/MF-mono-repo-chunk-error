{
  "name": "react-refresh-mf",
  "private": true,
  "workspaces": {
    "packages": [
      "host",
      "remote"
    ]
  },
  "scripts": {
    "start": "lerna run --scope @react-refresh-MF/* --parallel start",
    "build": "lerna run --scope @react-refresh-MF/* build",
    "serve": "lerna run --scope @react-refresh-MF/* --parallel serve",
    "clean": "lerna run --scope @react-refresh-MF/* --parallel clean"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
